
<%= render 'layouts/errors', :object => @tweet %>
<%= form_for(@tweet) do |f| %>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="actions"><%= f.submit "New Tweet" %></div>
<% end %>

